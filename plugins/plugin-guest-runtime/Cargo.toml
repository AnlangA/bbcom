[package]
name = "bbcom-plugin-guest-runtime"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
publish = false
description = "bounded no-WASI allocator support for BBCOM plugin components"
license = "MIT"

[workspace]

[lib]
crate-type = ["rlib"]
